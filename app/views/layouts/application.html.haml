%html.no-js{ lang: 'eng' }
  %head
    = render 'shared/head'
  %body{class: "#{params[:controller]} #{params[:action]}"}

    = render 'shared/header'

    = yield :header_fluid

    #main
      .container
        = yield

    = render 'shared/footer'
