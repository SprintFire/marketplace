#shop_about.card.card-block.col-md-4.col-md-offset-1
  .text-xs-center
  - if shop.profile_image?
    = image_tag(shop.profile_image.url)
  - else
    %img.img-circle.image_center{:alt => "Profile picture",  src: "http://placehold.it/100x100"}
    %p.about-title=shop.name
