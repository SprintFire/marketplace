%p= @product.name

%h2 Add a comment:
= form_for(@review, url: create_review_path(@review)) do |f|
  = f.hidden_field :product, value: @product
  %p
    = f.label :comment
    %br/
    = f.text_field :comment
  %p
    = f.submit
